/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['example.com'], // Add your image hosting domains if needed
  },
};

module.exports = nextConfig;
